#要求
总结对话记录提取出喜好的特点

#生成具体要求
按下面json生成，并且返回一个json，纯粹的返回json就好，不需要携带任何的md格式。仔细阅读json数据的属性的含义，如果没有提取到该属性的信息，则返回空字符串，不生成没提到的属性
```json
{
    "food":"喜欢的食物，值是个数组，里面存放用户喜好的食物种类，没提取到则为空数组",
    "sport":"喜欢的运动，值是个数组，里面存放用户喜好的食物种类，没提取到则为空数组",
}
```
 